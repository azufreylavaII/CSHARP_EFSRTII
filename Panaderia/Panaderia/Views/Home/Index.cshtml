@model List<Panaderia.Models.Producto>

@{
    ViewBag.Title = "Home Page";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <title>Panadería Artesanal - Inicio</title>
    <style>
        :root {
            --primary-color: #ff8c42;
            --secondary-color: #b22222;
            --background-color: #fff9f5;
            --text-color: #333;
        }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }

        .hero-section {
            position: relative;
            height: 100vh;
            background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url("/Content/Images/banner_pan.png");
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: white;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .hero-content {
            max-width: 800px;
            padding: 2rem;
        }

        .hero-title {
            font-size: 4.5rem;
            margin-bottom: 1rem;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .hero-subtitle {
            font-size: 1.8rem;
            margin-bottom: 2rem;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
        }

        .section-title {
            color: var(--secondary-color);
            text-align: center;
            margin: 3rem 0;
            font-size: 2.2rem;
            position: relative;
            padding-bottom: 1rem;
        }

            .section-title::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 50%;
                transform: translateX(-50%);
                width: 80px;
                height: 3px;
                background-color: var(--primary-color);
            }

        .historia-section {
            padding: 6rem 0;
            background-color: white;
        }

        .historia-content {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 2rem;
            text-align: center;
        }

        .historia-image {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            margin: 2rem auto;
            display: block;
            border: 3px solid var(--primary-color);
        }

        .process-section {
            padding: 4rem 0;
            background-color: var(--background-color);
        }

        .process-step {
            text-align: center;
            padding: 2rem;
            margin: 1rem 0;
        }

        .step-number {
            width: 40px;
            height: 40px;
            background-color: var(--primary-color);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-weight: bold;
        }

        .product-card {
            background: white;
            border-radius: 15px;
            padding: 1rem;
            margin-bottom: 2rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

            .product-card:hover {
                transform: translateY(-5px);
            }

        .product-image {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 1rem;
        }

        .product-title {
            font-size: 1.2rem;
            color: var(--text-color);
            margin: 0.5rem 0;
        }

        .product-price {
            color: var(--secondary-color);
            font-weight: bold;
            font-size: 1.3rem;
        }

        .feature-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .features-section {
            background-color: white;
            padding: 4rem 0;
            margin: 3rem 0;
        }

        .values-section {
            padding: 4rem 0;
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url("/Content/Images/valores_bg.jpg");
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: white;
        }

        .value-item {
            text-align: center;
            padding: 2rem;
        }

        .value-icon {
            font-size: 3rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        @@media (max-width: 768px) {
            .hero-title {
                font-size: 3rem;
            }

            .hero-subtitle {
                font-size: 1.4rem;
            }

            .historia-content {
                padding: 0 1rem;
            }
        }
    </style>
</head>
<body>
    <section class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">Panadería Artesanal</h1>
            <p class="hero-subtitle">Tradición y sabor en cada bocado</p>
        </div>
    </section>

    <section class="historia-section">
        <div class="container">
            <h2 class="section-title">Nuestra Historia</h2>
            <div class="historia-content">
                <img src="/Content/Images/panadero.jpg" alt="Fundador" class="historia-image">
                <p>Desde 1995, nuestra panadería ha sido el corazón del barrio, ofreciendo pan artesanal y productos horneados con las recetas tradicionales de la familia. Lo que comenzó como un pequeño horno en casa se ha convertido en un referente de la panadería artesanal en la ciudad.</p>
                <p>Mantenemos viva la tradición del pan artesanal, utilizando masa madre de cultivo y técnicas de fermentación lenta que dan como resultado productos de excepcional sabor y calidad.</p>
            </div>
        </div>
    </section>

    <section class="process-section">
        <div class="container">
            <h2 class="section-title">Nuestro Proceso</h2>
            <div class="row">
                <div class="col-md-4">
                    <div class="process-step">
                        <div class="step-number">1</div>
                        <h3>Selección de Ingredientes</h3>
                        <p>Escogemos cuidadosamente las mejores materias primas para garantizar la calidad de nuestros productos.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="process-step">
                        <div class="step-number">2</div>
                        <h3>Fermentación Lenta</h3>
                        <p>Permitimos que nuestras masas fermenten naturalmente durante largos períodos para desarrollar sabores complejos.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="process-step">
                        <div class="step-number">3</div>
                        <h3>Horneado Artesanal</h3>
                        <p>Cada pieza es horneada con precisión para lograr la textura y sabor perfectos.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="values-section">
        <div class="container">
            <h2 class="section-title" style="color: white;">Nuestros Valores</h2>
            <div class="row">
                <div class="col-md-4">
                    <div class="value-item">
                        <i class="fas fa-heart value-icon"></i>
                        <h3>Pasión</h3>
                        <p>Amamos lo que hacemos y eso se refleja en cada producto que elaboramos.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="value-item">
                        <i class="fas fa-leaf value-icon"></i>
                        <h3>Naturalidad</h3>
                        <p>Utilizamos ingredientes naturales y procesos tradicionales.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="value-item">
                        <i class="fas fa-users value-icon"></i>
                        <h3>Comunidad</h3>
                        <p>Creamos vínculos con nuestra comunidad a través del pan.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>



    <div class="container">
        <section id="productos">
            <h2 class="section-title">Nuestros Productos</h2>
            <div class="row">
                @foreach (var producto in Model)
                {
    <div class="col-md-3">
        <div class="product-card">
            <h3 class="product-title">@producto.Nombre</h3>
            <p class="product-price">S/ @producto.Precio.ToString("0.00")</p>
        </div>
    </div>            }
            </div>
        </section>
    </div>
</body>
</html>